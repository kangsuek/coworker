⏺ 테스트 중 3가지 개선 포인트를 발견했습니다.

  ---
  1. 🔴 Agent 작업 중 실시간 스트리밍 미표시

  현상: Researcher-1, 2가 작업하는 동안 Agent Channel에 텍스트가 실시간으로 흘러야 하는데, 완료될 때까지 "작업 중..." 고정 텍스트만 표시됨. 완료 후 전체 텍스트가 한꺼번에
   나타남.

  기대 동작: Agent가 출력하는 줄이 실시간으로 누적 표시 (SSE/폴링으로 content 필드 점진적 업데이트)

  ---
  2. 🟡 분류 단계 16초 소요

  현상: "(팀모드)" 프리픽스가 명시적으로 있음에도 LLM 분류 호출을 거쳐 약 16초 소요됨.

  개선 가능: "(팀모드)" 프리픽스를 규칙 기반으로 즉시 감지해 분류 CLI 호출을 생략하면 체감 시작 속도가 크게 향상됨. 현재 규칙 기반 분류 코드가 있다면 이 케이스를 먼저
  처리하는지 확인 필요.

@@@@@여기를 작업 중입니다@@@@@@@@

  "(팀모드)" 프리픽스가 명시적으로 있음에도 LLM 분류 호출을 거쳐 약 16초 소요됨.
  -> "(팀모드)" 프리픽스를 사용하더라도 agent들의 역할 및 어떤 agent들이 동시에 진행하고 어떤 agent가 나중에 동작해야 하는지 등의 순서를 조율하기 위해서는 LLM 분류 호출이 필요함.
  -> 이떄 사용하는 LLM모델은 Claude CLI는 Haiku, Gemini CLI는 Gemini3 flash를  각각 사용하는 것임.
  -> 이를 더욱 개선하기 위한 방법이 있다면 제안하여 research.md에 기록하세요.




  ---
  3. 🟡 Researcher의 웹 검색 불가 안내 미흡

  현상: Researcher-1 결과 첫 줄에 "웹 검색 권한 문제로 인해, 제 전문 지식을 기반으로..." 라고 출력됨. 실시간 정보(월식 일자 등)가 필요한 태스크에서 hallucination 가능성이
   있음.

  개선 가능: Researcher 프리셋 시스템 프롬프트에 "실시간 정보 조회가 불가능한 경우, 그 사실을 명확히 밝히고 한국천문연구원 등 신뢰 기관 확인을 안내하라"는 지침을 추가하면
   사용자가 결과 신뢰도를 올바르게 판단할 수 있음. (Researcher-2는 이를 잘 따랐으나 Researcher-1은 단순 사과 후 진행)

  ---
  요약

  ┌──────────┬───────────────────────────────────┬────────────────────┐
  │ 우선순위 │               항목                │        영향        │
  ├──────────┼───────────────────────────────────┼────────────────────┤
  │ 높음     │ Agent 실시간 스트리밍 표시        │ UX 체감 품질       │
  ├──────────┼───────────────────────────────────┼────────────────────┤
  │ 중간     │ (팀모드) 프리픽스 즉시 처리       │ 시작 속도 16초→1초 │
  ├──────────┼───────────────────────────────────┼────────────────────┤
  │ 낮음     │ Researcher 웹 검색 불가 안내 지침 │ 결과 신뢰도        │
  └──────────┴───────────────────────────────────┴────────────────────┘
